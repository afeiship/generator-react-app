import { Modal } from 'antd';
import AppBase, { $api } from 'components/scripts';

export default class extends React.Component {
  render() {
    return (
      <Modal className="<%= modal_name%>">
        USER_ADD + EXM
      </Modal>
    );
  }
}

